# LB_plugin 작업 지침

이 저장소는 RisuAI LightBoard 삽화 상태 플러그인과 호환 모듈의 배포 저장소다.

## 파일과 버전

- 배포 파일은 루트의 `lightboard_illust_status.js` 하나다. 버전별 새 파일을 만들지 않는다.
- 온라인 업데이트 호환성을 위해 `//@name lightboard-illust-status-v42`를 변경하지 않는다.
- 새 배포마다 `//@version`을 증가시키되 선언을 첫 512바이트 안에 유지한다.
- `//@update-url`의 저장소·브랜치·파일 경로를 변경하지 않는다.
- 작업 후 반드시 `verify.ps1`을 실행한다.
- `module/`의 `.charx` 바이너리를 직접 편집하지 않는다. 모듈 변경은 원본 작업 저장소에서 hermes-risuai-skills로 추출·재조립한 뒤 완성본을 교체한다.
- 백엔드 소스는 이 저장소에 두거나 `backend/`를 다시 만들지 않는다.
- 백엔드 확인 또는 수정이 필요하면 `E:\test3\comfyui_hooking_server`를 작업 대상으로 사용한다.

## 범위

- 플러그인은 설정, 신호 송수신, 서버 상태 표시만 담당한다.
- 채팅 메시지/인덱스, 이미지 파일/슬롯, 인레이 삽입을 플러그인에 구현하지 않는다.
- 생성·컨텍스트 필터·슬롯 삽입은 모듈 책임이다.
- 서버 수정이 필요하면 `E:\test3\comfyui_hooking_server`에서 수행한다. 서버 및 모듈은 사용자가 명시적으로 요청하지 않는 한 수정하지 않는다. 수정할 때는 플러그인 역할 경계를 침범하지 않는지 함께 검증한다.
- import 직후 또는 주소 저장 전 네트워크 요청을 추가하지 않는다.
- 생성 작업이 없을 때 상시 폴링을 추가하지 않는다.
- 대시보드 입력 포커스 중 렌더링으로 입력값을 덮지 않는다.

세부 배경과 회귀 항목은 `DEVELOPMENT.md`를 먼저 읽는다.
