# 개발 규칙과 주의점

## 역할 경계

플러그인의 역할은 다음으로 제한합니다.

- 서버 주소 저장 및 연결 확인
- 백엔드가 최소 범위로 제공한 봇 이름과 현재 활성 봇을 조회·변경
- Risu generation 출력을 바꾸지 않고 그대로 반환하면서 상태 조회를 활성화
- 서버의 세션 진행 상태를 읽어 대시보드와 플로팅 창에 표시
- 모듈의 고유 버튼 동작 코드를 감지하되 payload의 인덱스·슬롯 값은 해석하지 않음

플러그인이 해서는 안 되는 일은 다음과 같습니다.

- 채팅 메시지나 채팅 인덱스 읽기·추정·저장
- 이미지 파일 또는 이미지 슬롯 직접 조회·수신·삽입
- Risu 메시지에 결과를 직접 추가
- 서버 대신 후보 슬롯을 전수 검색
- Risu 내장 LLM을 호출하는 재생성 경로 구현

이미지별 `편하게 수정`은 정적 이미지에서만 노출합니다. 모듈이 입력창·세션·슬롯 교체를 맡고, 백엔드가 기존 LLM 편집과 수정 재생성을 맡습니다. 애니메이션 슬롯은 모듈 UI와 동작 핸들러 및 백엔드에서 모두 거절하며, 플러그인은 노출된 정적 이미지 편집 버튼을 상태 감시 신호로만 취급합니다.

이미지 생성, 컨텍스트 필터 적용, 슬롯 삽입, 인레이 처리는 정상 작동하는 모듈이 담당합니다. 플러그인과 모듈이 각자 채팅 인덱스를 계산하면 메시지 삭제·재전송 뒤 서로 다른 대상을 바라보게 됩니다.

### onOutput 트랜지언션 격리

Risu의 onOutput 모듈 코드는 커밋 전 트랜지언션 클론에서 실행됩니다. 실행 중 모듈의 `addChat`·`setChatVar`는 live DB에서 보이지 않고, live DB에 플러그인이 쓴 값도 모듈의 `getChatVar`에서 보이지 않았습니다. 따라서 다음 설계를 다시 도입하지 마십시오.

- 임시 채팅 메시지를 플러그인이 찾아 세션을 연결하는 방식
- scriptstate를 플러그인과 모듈 사이의 실시간 relay로 쓰는 방식
- 플러그인이 live 채팅 인덱스를 찾은 뒤 onOutput 모듈에 전달하는 방식

v42는 외부 서버와 모듈의 `generateImage(..., 'inlay')` 경로를 사용해 이 격리를 피합니다. 플러그인은 그 과정에 개입하지 않고 상태만 관찰합니다.

## 온라인 업데이트 불변 조건

- 배포 파일 경로는 `main/lightboard_illust_status.js`로 고정합니다.
- `//@name lightboard-illust-status-v42`는 업데이트 식별자이므로 절대 바꾸지 않습니다.
- 표시용 `//@display-name`은 변경해도 되지만 특별한 이유가 없으면 유지합니다.
- `//@version`은 매 배포마다 증가시킵니다. `42.0.5` 다음은 예를 들어 `42.0.6`입니다.
- `//@version` 선언은 파일 첫 512바이트 안에 있어야 합니다.
- `//@update-url`은 HTTPS GitHub raw URL이어야 하며 파일명·브랜치·저장소를 임의로 바꾸지 않습니다.
- 배포 전에 `YOUR_GITHUB_ID`가 남아 있지 않아야 합니다.
- 원격 호스트는 CORS와 HTTP Range 요청을 허용해야 합니다. GitHub raw 주소를 사용합니다.

RisuAI는 업데이트 확인 시 원격 파일의 앞부분을 Range 요청하고 `//@version`을 비교합니다. 원격 버전이 더 높을 때만 녹색 업데이트 `+`를 표시하며, 설치할 때 기존 플러그인의 `//@name`과 원격 파일의 이름이 같은지 검사합니다.

업데이트 import는 플러그인 데이터 객체를 새로 만들기 때문에 `//@arg`에서 사용자가 조절한 값이 초기화될 수 있습니다. 핵심 영속 설정을 헤더 인자에만 두지 마십시오. 현재 서버 주소처럼 플러그인 저장소와 캐릭터 변수에 별도로 저장하는 방식을 유지합니다.

## 과거에 반복된 문제와 방지책

### 가져오자마자 health 요청

플러그인 import 직후 또는 서버 주소 저장 전에는 네트워크 요청을 시작하지 않습니다. 자동 상태 확인은 생성 감지 뒤에만 활성화하고, 작업이 끝나면 중지합니다.

### 생성 시작 전 연결 제한

일반 generation, CALL1 전체 재생성, raw 전체 생성, 이미지별 raw 재생성, 정적 이미지별 편하게 수정은 모듈에서 실제 `generateImage` 호출 전에 서버 health를 확인합니다. 연결 확인은 5초 타이머와 경합시키며, 타임아웃이나 비정상 응답이면 오류를 표시하고 생성 호출로 진행하지 않습니다. 늦게 도착한 응답이 종료된 작업을 다시 시작해서도 안 됩니다.

### 자동·수동 작동 모드

모듈의 `toggle_lb-xnai.enabled` 값은 `0=끄기`, `1=자동`, `2=수동`입니다. 기존 `켜기` 설치본의 값 `1`은 자동 모드로 그대로 호환해야 합니다. 수동 모드의 `onOutput`은 최신 답장에 `lb-lazy` 삽화 메뉴만 추가하고 서버 health·manifest·이미지 생성을 호출하지 않습니다. 사용자가 메뉴의 생성 버튼을 눌렀을 때만 기존 연결 확인과 CALL1/2/3 생성 경로로 진입합니다.

### 전 슬롯 애니메이션 이미지의 영속 fallback

백엔드 세션 metadata는 디스크에 남지만 이미지 bytes는 메모리에만 있으므로 서버 재시작이나 임시 HTTPS 주소 변경 뒤 직접 이미지 URL은 복원되지 않습니다. 키비주얼과 일반 장면은 애니메이션용 서버 URL만 저장하지 말고, 같은 `generateImage` 결과의 Risu PNG 인레이를 첫 프레임 fallback으로 함께 보존해야 합니다. 서버 URL 이미지는 fallback 위에 겹쳐 표시해 정상일 때 서버가 보낸 PNG/GIF/animated AVIF/WebP를 표시하고, URL 또는 포맷 디코딩이 실패하면 로컬 인레이가 보이게 유지합니다. 서버는 실제 바이트에 맞춰 `image/png`, `image/gif`, `image/avif`, `image/webp` MIME을 반환해야 합니다.

soya-v51은 v50과 마찬가지로 애니메이션 슬롯에만 `loading="eager" decoding="sync"`를 사용하고 정적 이미지는 `lazy/async`를 유지합니다. 모듈의 이미지 URL에는 내용 교체 때마다 바뀌는 `?v=생성시각-실행번호`가 반드시 있어야 합니다. 백엔드는 이 형식의 버전 쿼리가 있는 200 이미지 응답에만 `Cache-Control: private, max-age=3600, immutable`을 반환하며, 버전이 없거나 잘못된 요청과 모든 오류 응답은 `no-store`를 유지해야 합니다. 이 캐시는 공유 CDN이 아닌 사용자 브라우저에만 저장되고 브라우저의 일반 캐시 정리 대상이어야 합니다.

soya-v53은 오래된 채팅의 서버 URL이 실패했을 때 WebView가 그리는 대체 아이콘을 `.lb-xnai-server-live` 레이어 밖으로 들여쓰고 `overflow: hidden`으로 잘라냅니다. 이 처리는 정상 서버 이미지 픽셀과 애니메이션, `loading`·`decoding` 속성을 바꾸지 않으며 아래의 Risu PNG fallback도 가리지 않아야 합니다. 인라인 `onerror`는 Risu의 DOM 정화 과정에서 제거되므로 다시 도입하지 않습니다.

soya-v54는 KEYVISUAL이 일반 장면과 다른 버튼·컨테이너 경로를 사용해 v53의 CSS 잘라내기가 WebView 대체 UI를 완전히 숨기지 못한 예외를 처리합니다. `editDisplay` 렌더 시점에 `slot == '-1'`인 KEYVISUAL의 `.lb-xnai-server-live` HTML `<img>`만 SVG `<image>` 레이어로 바꿉니다. 실패한 SVG 이미지는 엑박 대체 UI를 그리지 않고 투명하게 남아 아래 Risu fallback을 노출합니다. v48~v53이 저장한 기존 마크업의 속성 순서를 호환하며, 저장 채팅을 변경하지 않고 키비주얼 인라인과 전체화면에만 적용해야 합니다. 일반 장면·에셋의 HTML `<img>` 경로는 변경하지 않습니다.

soya-v55는 v54의 SVG `<image>`가 일부 Risu WebView에서 래스터·애니메이션을 비정상적으로 확대·합성하는 회귀를 제거합니다. KEYVISUAL의 라이브 레이어만 `<span style="background-image:...">`으로 변환하고 `background-size: cover`로 기존 크롭을 유지합니다. CSS 배경 URL은 실패해도 대체 아이콘을 생성하지 않으며, 정상 애니메이션은 SVG가 아닌 브라우저의 일반 이미지 디코딩·합성 경로를 사용해야 합니다. Risu의 DOM 정화는 일반 상태에서 `style` 배경 URL을 보존하고 `hideAllImages`일 때는 제거하므로 기존 설정 의미도 유지됩니다.

soya-v56은 표시 이름을 `라이트보드 삽화 4.1.0-soya-v56`으로 갱신하고 채팅 아이콘을 128×128 흰색 `S` 아이콘으로 교체합니다. Lua의 버전 식별자만 v56으로 올리며 생성·컨텍스트 필터·슬롯 삽입·에셋 리롤·KEYVISUAL 렌더링 동작은 v55와 동일하게 유지합니다.

soya-v57은 채팅 메뉴의 CSS 아이콘을 `S`, 버튼 이름을 `SOYA 삽화`, 설정 그룹 이름을 `S SOYA 삽화`로 통일합니다. LightBoard 4.1.0이 미리 만든 마지막 완성 LBDATA 블록이 있으면 `<lb-lazy id="lb-xnai" />`를 그 안에 삽입하고, 없을 때만 독립된 표준 LBDATA 블록을 추가합니다. 4.1.0 코어 트리거로 전체 교체하면 현재 `lb-xnai-*` 서버 직접 생성·전체 재생성·에셋 리롤 핸들러가 사라지므로, 해당 핸들러와 생성 경로는 v56 그대로 보존합니다.

soya-v58은 `S SOYA 삽화` 채팅 메뉴의 `S` 의사 요소만 진한 초록색(`#15803d`) 배경과 흰색 굵은 글자의 작은 배지로 표시합니다. `SOYA 삽화` 버튼 본문, 설정 그룹 이름, 카드 아이콘, LBDATA 4.1 호환 처리와 모든 생성 핸들러는 v57 그대로 유지합니다.

soya-v59는 v58에서 잘못 바꾼 채팅 메뉴를 기존 `✦ 삽화`로 복원하고, 모듈 설정 그룹 제목만 `🌱 SOYA 삽화`로 변경합니다. Risu 그룹 제목은 `Accordion`의 이스케이프된 일반 문자열로 렌더되어 `.charx` 이미지나 부분 HTML/CSS 배지를 삽입할 수 없으므로 씨앗 이모지를 식별자로 사용합니다. 카드 아이콘, LBDATA 4.1 호환 처리와 모든 생성 핸들러는 보존합니다.

### soya-v60 완성 이미지 전송과 저장 병렬 처리

첫 `generateImage()`가 반환한 인레이는 재사용하고, manifest의 나머지 슬롯은 `receiveCachedInlays`가 최대 3개까지 동시에 회수합니다. `generateImage()` 한 호출에는 다운로드와 Risu 영속 저장이 모두 포함됩니다. 호출별 저장 완료를 기다리는 동안 다른 호출의 전송·저장이 진행되며, 먼저 끝난 작업이 바로 다음 슬롯을 시작합니다. 슬롯 번호를 기준으로 결과를 연결하므로 완료 순서가 달라도 이미지가 섞이지 않아야 합니다.

일반 생성·전체 재생성의 `discoverSessionInlays`와 에셋 리롤의 완성 에셋 회수만 이 경로를 사용합니다. 최초 생성 요청, RAW 전체 생성, 개별 생성·수정의 호출 방식은 유지합니다. 실패를 확인하면 새 슬롯 회수를 멈추고 이미 시작한 호출은 모두 기다린 다음 오류를 전파합니다. 모든 저장이 끝나기 전에 `running` 보호를 풀거나 대상 응답에 결과를 적용하지 마십시오. 실패 로그에는 세션·슬롯·Lua traceback을 남깁니다.

PocketRisu 1.11.2에서 사용 중인 Wasmoon 1.16.0과 해당 배포본의 `async` wrapper로 동시 처리, 완료 순서 역전, 첫 인레이 재사용, 실패 후 진행 중 저장 대기를 검증했습니다. 다운로드·저장 시간은 테스트용 Promise로 재현했으며, 실제 서비스의 성능 개선 수치를 측정한 결과는 아닙니다. 실제 `discoverSessionInlays` 실행에서는 manifest 1회 조회, 슬롯별 서버 URL·영속 fallback, 애니메이션·에셋 metadata, eager/sync 및 lazy/async 속성을 확인했습니다.

### 대시보드 입력 중 화면 갱신

서버 상태 렌더링이 입력칸을 다시 만들면 HTTPS 주소를 타이핑하는 동안 값이 초기화됩니다. 현재의 draft/focus 보호 로직을 제거하지 마십시오. 입력 포커스 중에는 상태 갱신이 폼 값을 덮지 않아야 합니다.

### 채팅 인덱스 결합

플러그인은 메시지 삭제·재전송에 취약한 채팅 인덱스를 전달하지 않습니다. 모듈 버튼은 고정된 동작 식별자만 감지하며 payload의 인덱스를 해석하지 않습니다.

### 내장 LLM 경로 진입

generation 및 regeneration 제어 신호를 Risu가 알고 있는 일반 이미지 메타 타입으로 위장하면 백엔드 또는 내장 LLM 경로로 들어갈 수 있습니다. 백엔드가 해석하지 않는 전용 신호와 모듈의 generation 경로를 유지하십시오.

### 슬롯 전수 조회와 오류 토스트

없는 이미지 슬롯을 Risu 파일 API로 하나씩 읽으면 `cannot read file name` 오류가 플로팅됩니다. 플러그인은 이미지 슬롯을 조회하지 않습니다. 서버가 알려 주는 상태·짧은 조회 키만 다루며, 실제 슬롯 처리는 모듈에 남깁니다.

모듈의 후보 슬롯은 `컨텍스트 필터 → 문단 경계 계산 → 최대 64개 균등 선택` 순서여야 합니다. 필터 전 문단에 먼저 슬롯을 만들면 제외 구간에도 슬롯이 분산되고 후보가 100개 이상으로 불어날 수 있습니다.

### 세션과 슬롯 혼동

서버의 64개 후보 슬롯 압축은 전체 슬롯 번호의 최댓값이 63이라는 뜻이 아닙니다. 실제 슬롯 번호가 70, 75처럼 보여도 후보 목록 개수가 제한 안이면 정상입니다. 모듈은 64자 내부 세션 ID와 별도의 24자 lowercase hex 조회 키를 사용합니다. 짧은 키는 `/s/{key}`에서 실제 슬롯 배열만 가져오기 위한 식별자이며 채팅 인덱스가 아닙니다.

`/s/{key}`의 기존 응답은 이전 모듈 호환용 숫자 배열로 유지합니다. soya-v49 이상은 `/s/{key}?m=1`을 사용하며 `slots`와 `animated`를 검증하고, soya-v51 이상은 여기에 `assets` 슬롯 배열도 검증합니다. 이 조회는 생성·재생성 결과를 적용할 때 한 번만 수행하고 렌더링 중 추가 요청이나 상시 폴링을 만들지 않습니다. 모듈은 `animated` 슬롯에만 `animated="1"`을 저장해 `편하게 수정` 버튼을 숨기고, `assets` 슬롯에만 `asset="1"`을 저장해 별도의 에셋 표시 섹션을 적용합니다. soya-v52의 연결 확인은 bridge protocol 10과 `asset_display_metadata`, `asset_reroll`을 모두 요구해야 합니다.

### 에셋만 리롤 격리

soya-v52의 `에셋만 리롤`은 현재 응답에서 일반 삽화 슬롯을 예약하고, 최초 생성과 동일한 `_select_original_asset_outputs` 및 `illustration_original_asset` LLM 작업으로 업로드 원본 에셋과 위치를 다시 선택합니다. 새 선택이 전부 검증되고 원본 파일 bytes를 읽은 뒤에만 세션과 Risu 메시지를 교체합니다. 일부 실패, 슬롯 충돌, 선택 중 대상 응답 변경 시 기존 표시를 유지합니다.

이 동작의 `generateImage()` 호출은 Risu 로컬 인레이를 만드는 결과 운반 통로일 뿐입니다. 서버는 `action=asset_reroll`을 일반 RAW descriptor 생성보다 먼저 가로채야 하며, `illustration` 타입의 Comfy/hybrid 큐, CALL1/2/3, 저장 RAW 생성으로 폴백해서는 안 됩니다. 일반 삽화 노드는 그대로 보존하고 기존 에셋 노드만 제거한 뒤, 현재 본문의 필터 적용 후 문단 슬롯에 새 에셋을 원자적으로 삽입합니다.

### 플로팅 UI 조건

다음 여섯 동작은 진행창을 표시할 수 있어야 합니다.

- 일반 generation
- CALL1부터 시작하는 응답 전체 재생성
- raw 전체 생성
- 에셋만 리롤
- 이미지별 raw 재생성
- 정적 이미지별 편하게 수정

진행창 설정이 꺼져 있어도 생성 연동 자체는 작동해야 합니다. UI z-index는 provider-manager 플로팅 창과 겹치지 않도록 현재 값을 함부로 통합하지 않습니다.

## 수정 후 최소 회귀 확인

- import 직후 네트워크 요청이 없는가
- 서버 주소 입력 도중 값이 초기화되지 않는가
- 주소 저장 전에는 요청하지 않는가
- 생성 시작 전 연결 확인이 5초 안에 끝나며 실패 시 `generateImage`를 호출하지 않는가
- generation과 전체·raw·에셋 리롤·개별 재생성 모두 필요한 경우 진행창이 뜨는가
- 기존 모드 값 `1`이 자동 생성을 계속 수행하는가
- 수동 모드 값 `2`에서 답장 직후 모듈이 네트워크·이미지 생성을 시작하지 않고 삽화 메뉴만 추가하는가
- 수동 모드의 삽화 생성 버튼이 연결 확인 뒤 CALL1/2/3 생성으로 진입하는가
- 에셋만 리롤이 최초 원본 에셋 선택 LLM 경로를 재사용하고 일반 삽화·Comfy 큐에는 진입하지 않는가
- 에셋 리롤 성공 시 일반 삽화를 보존하면서 에셋과 슬롯만 바뀌고, 실패나 대상 응답 변경 시 기존 표시가 유지되는가
- 정적 이미지에는 `편하게 수정` 버튼이 보이고 기존 편집 경로가 작동하는가
- 애니메이션 이미지에는 `편하게 수정` 버튼이 없으며 모듈·백엔드가 직접 호출도 거절하는가
- 키비주얼과 일반 장면 모두 서버 원본 URL과 Risu 로컬 PNG fallback이 함께 저장되는가
- 만료·누락된 서버 URL에서 fallback은 보이되 키비주얼의 인라인·전체화면과 일반 장면에 브라우저 엑박 아이콘이 겹치지 않는가
- 정상 KEYVISUAL의 PNG/GIF/animated AVIF/WebP가 흐림·확대·조각남 없이 표시되고 애니메이션이 실제로 진행하는가
- 애니메이션 이미지는 eager/sync, 정적 이미지는 lazy/async 속성으로 저장되는가
- 버전 쿼리가 있는 정상 이미지 응답만 1시간 private 캐시되고 오류 응답은 `no-store`인가
- 진행창을 꺼도 모듈 동작이 유지되는가
- 완료 후 폴링이 멈추는가
- 메시지·인덱스·이미지 API가 새로 들어가지 않았는가
- 원격 버전이 높을 때만 업데이트 버튼이 뜨는가
