# Changelog

## 42.0.32 / module soya-v59

- 모듈 설정 그룹 이름을 `🌱 SOYA 삽화`로 변경
- v58에서 잘못 변경한 채팅 메뉴 이름과 아이콘을 기존 `✦ 삽화`로 복원
- Risu 그룹 제목은 일반 문자열만 지원하므로 별도 PNG·부분 HTML 대신 씨앗 이모지를 그룹 식별자로 사용
- LBDATA 4.1 호환 처리, 서버 직접 생성·재생성·에셋 리롤·개별 편집 핸들러와 카드 아이콘 보존
- 변경된 모듈을 `module/라이트보드  삽화 4.1.0-soya-v59.module.charx` 새 파일로 추가하고 v58 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.23`, 대시보드 배포 버전을 `v42.0.32`로 동기화

## 42.0.31 / module soya-v58

- 채팅에 표시되는 `S SOYA 삽화` 메뉴에서 `S`만 진한 초록색(`#15803d`) 배경과 흰색 굵은 글자의 배지로 변경
- `SOYA 삽화` 버튼 본문과 `S SOYA 삽화` 설정 그룹 이름은 그대로 유지
- LBDATA 4.1 호환 처리, 기존 서버 직접 생성·전체 재생성·에셋 리롤·개별 편집 핸들러와 카드 아이콘 보존
- 변경된 모듈을 `module/라이트보드  삽화 4.1.0-soya-v58.module.charx` 새 파일로 추가하고 v57 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.22`, 대시보드 배포 버전을 `v42.0.31`로 동기화

## 42.0.30 / module soya-v57

- 채팅 메뉴의 장식 아이콘을 `S`, 버튼 이름을 `SOYA 삽화`, 설정 그룹 이름을 `S SOYA 삽화`로 변경해 남아 있던 `삽화 v32` 표시 제거
- LightBoard 4.1.0이 먼저 만든 마지막 완성 `[LBDATA START]`/`[LBDATA END]` 블록 안에 삽화 마커를 삽입해 빈 블록과 삽화 전용 블록이 인접하는 렌더링 오류 방지
- 기존 `lb-xnai-*` 서버 직접 생성·전체 재생성·에셋 리롤·개별 편집 핸들러와 독립 실행용 표준 LBDATA fallback 보존
- 변경된 모듈을 `module/라이트보드  삽화 4.1.0-soya-v57.module.charx` 새 파일로 추가하고 v56 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.21`, 대시보드 배포 버전을 `v42.0.30`으로 동기화

## 42.0.29 / module soya-v56

- 모듈 표시 이름을 `라이트보드 삽화 4.1.0-soya-v56`으로 갱신
- 채팅에 표시되는 기존 `?` 아이콘을 128×128 흰색 `S` 아이콘으로 교체
- Lua와 모듈 메타데이터의 내부 버전 식별자를 `soya-v56`으로 통일하고 생성·표시 동작은 v55와 동일하게 보존
- 변경된 모듈을 `module/라이트보드  삽화 4.1.0-soya-v56.module.charx` 새 파일로 추가하고 v55 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.20`, 대시보드 배포 버전을 `v42.0.29`로 동기화

## 42.0.28 / module soya-v55

- v54의 KEYVISUAL SVG `<image>` 레이어가 Risu WebView에서 래스터·애니메이션을 흐리게 확대하거나 조각내던 회귀 수정
- KEYVISUAL의 상단 서버 레이어를 SVG 대신 CSS `background-image` span으로 변환해 정상 PNG/GIF/animated AVIF/WebP는 일반 이미지 디코딩·합성 경로로 표시
- 서버 URL이 만료·누락되면 배경이 아무것도 그리지 않아, 엑박과 SVG 왜곡 없이 아래 Risu PNG fallback만 노출
- 149프레임 GIF의 시간별 페인트 변화, 실패 URL에서 라이브 `<img>`·SVG 부재, fallback과 레이어 크기 일치를 브라우저로 검증
- 저장된 채팅과 일반 장면·에셋·생성·재생성 경로, 서버·백엔드는 변경하지 않음
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v55.module.charx` 새 파일로 추가하고 v54 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.19`, 대시보드 배포 버전을 `v42.0.28`로 동기화

## 42.0.27 / module soya-v54

- KEYVISUAL만 별도의 버튼·컨테이너 경로로 렌더되어 v53의 CSS 잘라내기로는 WebView 엑박 대체 UI가 남던 차이를 반영
- v48 이후 저장된 KEYVISUAL의 상단 서버 `<img>`를 표시 시점에만 SVG `<image>` 레이어로 변환해, URL 만료·누락 시 엑박 없이 아래 Risu PNG fallback만 표시
- 저장된 채팅 마크업은 다시 쓰지 않고, 일반 장면·에셋·생성·재생성 경로와 서버·백엔드는 변경하지 않음
- KEYVISUAL 인라인과 전체화면 다이얼로그가 같은 안전 레이어를 쓰도록 하고, 정상 PNG/GIF/animated AVIF/WebP와 영구 fallback을 보존
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v54.module.charx` 새 파일로 추가하고 v53 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.18`, 대시보드 배포 버전을 `v42.0.27`로 동기화

## 42.0.26 / module soya-v53

- 오래된 채팅의 서버 원본 URL이 만료·누락되었을 때 정상 로컬 PNG fallback 위에 WebView 엑박 아이콘이 겹치던 문제 수정
- 실패한 `.lb-xnai-server-live`의 대체 UI만 큰 음수 들여쓰기로 레이어 밖에 두고 `overflow: hidden`으로 잘라, 저장된 채팅을 다시 쓰지 않아도 새 모듈 CSS가 즉시 적용되도록 구성
- 정상 PNG/GIF/animated AVIF/WebP 표시, Risu 로컬 fallback, animated 전용 eager/sync와 정적 lazy/async, 버전 URL 캐시 계약은 그대로 보존
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v53.module.charx` 새 파일로 추가하고 v52 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.17`, 대시보드 배포 버전을 `v42.0.26`으로 동기화

## 42.0.25 / module soya-v52

- 답장 삽화 메뉴에 `🎲 에셋만 리롤` 버튼을 추가하고, 기존 일반 삽화는 유지한 채 업로드 원본 에셋과 삽입 슬롯만 다시 선택
- 에셋 리롤도 최초 생성과 동일한 서버 `_select_original_asset_outputs` 및 `illustration_original_asset` LLM 작업을 사용하며, 일반 RAW descriptor·Comfy/hybrid 이미지 큐로 폴백하지 않도록 전용 `asset_reroll` transport로 격리
- 일반 삽화 슬롯을 예약한 뒤 현재 본문 후보 슬롯에서 에셋 위치를 다시 고르고, 선택·원본 파일 로드가 전부 성공한 경우에만 서버 세션과 Risu 메시지를 원자적으로 교체
- 실패하거나 선택 중 대상 응답이 바뀌면 기존 에셋과 일반 삽화를 그대로 유지하고 원인을 서버 cmd·Risu 알림·통합 큐 이력에 기록
- 브리지 protocol 10의 `asset_reroll` 기능 플래그를 추가하고 플러그인이 새 버튼 신호를 진행 상태 감시에 포함
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v52.module.charx` 새 파일로 추가하고 v51 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.16`, 대시보드 배포 버전을 `v42.0.25`로 동기화

## 42.0.24 / module soya-v51

- 서버의 `/s/{key}?m=1` 응답에 `original_asset` 슬롯 번호를 담는 `assets` 배열을 추가하고 브리지 protocol 9의 `asset_display_metadata` 기능으로 명시
- 모듈이 에셋 슬롯에 `asset="1"` 메타데이터를 보존해 일반 장면과 구분하고, 자동 생성·전체 생성·개별 재생성·편하게 수정에서도 구분을 유지
- 모듈 설정에 별도 `🗂️에셋 표시` 섹션을 추가해 원본 비율을 기본으로 가로세로비, 최대 너비(기본 600px), 정렬을 독립 조절
- 모듈과 플러그인의 연결 확인에서 protocol 9 및 `asset_display_metadata`를 요구해 구형 서버에서는 생성 전에 명확히 중단
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v51.module.charx` 새 파일로 추가하고 v50 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.15`, 대시보드 배포 버전을 `v42.0.24`로 동기화

## 42.0.23 / module soya-v50

- 버전 쿼리가 있는 정상 이미지 응답만 브라우저에 1시간 private 캐시하도록 백엔드 계약을 변경해 채팅 DOM 재렌더 때 animated AVIF/WebP/GIF를 다시 내려받는 문제 완화
- 잘못된 버전 쿼리, 준비 전·누락·오류 응답은 계속 `no-store`로 유지해 실패 응답이나 변경 가능한 URL이 캐시되지 않도록 제한
- 모듈은 애니메이션 슬롯에만 `loading="eager" decoding="sync"`를 사용하고, 정적 이미지는 기존 `lazy/async`를 유지
- 서버 원본 이미지 아래의 Risu PNG fallback과 애니메이션 편집 차단은 그대로 보존
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v50.module.charx` 새 파일로 추가하고 v49 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.14`, 대시보드 배포 버전을 `v42.0.23`으로 동기화

## 42.0.22 / module soya-v49

- 브리지 protocol 8의 `/s/{key}?m=1` opt-in 확장으로 기존 슬롯 배열과 함께 애니메이션 슬롯 번호를 한 번에 조회
- 정적 이미지에는 기존 `✎ 편하게 수정` 버튼과 백엔드 LLM 편집·수정 재생성 경로를 유지
- GIF·animated AVIF/WebP 등 애니메이션 슬롯에는 편집 버튼을 표시하지 않고, 모듈 동작 코드와 백엔드 큐에서도 직접 호출을 차단
- 기존 `/s/{key}` 숫자 배열 응답은 그대로 유지해 soya-v48 이하 모듈과의 호환성을 보존
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v49.module.charx` 새 파일로 추가하고 v48 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.13`, 대시보드 배포 버전을 `v42.0.22`로 동기화

## 42.0.21 / module soya-v48

- 키비주얼에만 적용하던 서버 원본 이미지 URL + Risu 로컬 PNG fallback 구조를 모든 일반 장면 슬롯으로 확장
- 자동 생성, 저장된 RAW 전체 생성, 이미지별 RAW 재생성, 편하게 수정 모두 동일한 전 슬롯 미디어 마크업을 사용하도록 통일
- 서버 URL이 PNG/GIF/animated AVIF/WebP 중 실제 응답을 그대로 표시하고, URL 만료·서버 재시작·포맷 디코딩 실패 시 로컬 첫 프레임을 유지하도록 구성
- 현재 백엔드의 응답 MIME 판별은 PNG/GIF만 지원하므로 animated AVIF/WebP 전송 전 서버 보완이 필요함을 문서화하고 서버 소스는 변경하지 않음
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v48.module.charx` 새 파일로 추가하고 v47 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.12`, 대시보드 배포 버전을 `v42.0.21`로 동기화

## 42.0.20 / module soya-v47

- 모듈 작동 모드를 `끄기 / 자동 / 수동`으로 확장하고, 기존 `켜기` 값은 같은 인덱스의 `자동`으로 호환
- 수동 모드에서는 답장 완료 후 모듈이 서버 health·manifest 요청이나 이미지 생성을 시작하지 않고 해당 답장에 삽화 메뉴만 표시
- 수동 삽화 버튼과 기존 전체·개별 생성 버튼을 플러그인의 동일한 고정 동작 신호로 감시
- GIF 키비주얼의 서버 URL 아래에 동일 결과의 Risu 로컬 인레이를 fallback으로 보존해 서버 재시작·임시 주소 만료 뒤 오래된 채팅이 검은 박스로 남는 문제 방지
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v47.module.charx` 새 파일로 추가하고 v46 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.11`, 대시보드 배포 버전을 `v42.0.20`으로 동기화

## module soya-v46

- 키비주얼 슬롯 `-1`만 서버 이미지 URL로 직접 표시해 GIF 등 애니메이션 이미지가 Risu 인레이 PNG 변환으로 정지하지 않도록 변경
- 일반 장면 슬롯은 기존 Risu 인레이 경로를 유지하고, 자동 생성·전체 생성·개별 재생성·편하게 수정에서 같은 분기 규칙 적용
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v46.module.charx` 새 파일로 추가하고 v45 배포본은 보존
- 플러그인과 서버는 변경하지 않음

## 42.0.19

- 이미지 도착으로 채팅 DOM이 다시 렌더될 때 이미지별 `편하게 수정` 감시용 전역 클릭 리스너가 중복 등록되던 문제 수정
- 모듈 버튼마다 리스너를 붙이는 대신 문서 전역 포인터 리스너 하나에서 실제 버튼 좌표를 확인해 상태 감시를 시작하도록 변경
- 같은 캐릭터에서 이미지 DOM만 갱신된 경우 캐릭터 서버 주소 동기화를 건너뛰어 불필요한 UI 재구성을 방지
- 플러그인과 대시보드 표시 버전을 `v1.0.10`, 대시보드 배포 버전을 `v42.0.19`로 동기화

## 42.0.17

- 일반 generation, CALL1부터 다시 그리기, RAW 전체 다시 그리기, 그림별 RAW 재생성, 편하게 수정 시작 전에 기존 서버 health 경로로 연결을 확인
- 연결 확인은 최대 5초만 기다리고, 타임아웃·요청 실패·비정상 health 응답이면 즉시 오류를 표시한 뒤 `generateImage`를 호출하지 않도록 변경
- 5초 뒤 늦게 도착한 연결 응답은 무시해 중단된 생성 작업이 뒤늦게 재개되지 않도록 보장
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v45.module.charx` 새 파일로 추가하고 v44 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.8`, 대시보드 배포 버전을 `v42.0.17`로 동기화

## 42.0.16

- 플러그인 대시보드에서 백엔드의 봇 이름 목록과 현재 `bot_selected`를 확인하고 선택 즉시 변경하는 UI 추가
- 이미지별 호버의 오래된 이미지 정리 버튼을 `✎ 편하게 수정`으로 교체하고 수정 방향 입력창을 추가
- 모듈의 수정 요청을 백엔드 기존 편하게 수정 LLM 및 수정 재생성 큐로 전달해 해당 세션 슬롯 이미지만 교체
- 백엔드 브리지 protocol 7의 최소 봇 선택 API와 편하게 수정 transport를 사용하며 플러그인은 메시지·인덱스·이미지를 계속 다루지 않음
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v44.module.charx` 새 파일로 추가하고 v43 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.7`, 대시보드 배포 버전을 `v42.0.16`으로 동기화

## 42.0.15

- 백엔드가 큐 취소나 생성 실패를 `error`로 확정한 세션을 활성 작업에서 제외해 플로팅 진행창이 자동으로 닫히도록 수정
- 향후 `failed`, `cancelled`, `canceled`, `missing` 상태도 종료 상태로 처리하고, `ready + regenerating` 재생성은 계속 활성 작업으로 표시
- 플러그인과 대시보드 표시 버전을 `v1.0.6`, 대시보드 배포 버전을 `v42.0.15`로 동기화

## 42.0.14

- `soya-v42`의 결과 조회에 남아 있던 16개 제한을 키비주얼 1개와 후보 슬롯 최대 64개를 합친 65개로 수정
- 17개 이상 슬롯 매니페스트가 `RESULT_DISCOVERY`에서 `invalid manifest result count`로 거부되던 문제 수정
- 변경된 모듈을 `module/라이트보드  삽화 3.4.1-soya-v43.module.charx` 새 파일로 추가하고 v42 배포본은 보존
- 플러그인과 대시보드 표시 버전을 `v1.0.5`, 대시보드 배포 버전을 `v42.0.14`로 동기화

## 42.0.13

- 서버와 플러그인의 슬롯 매니페스트 상한을 키비주얼 포함 최대 65장으로 통일
- 준비 완료 세션 하나의 슬롯 메타데이터가 잘못되어도 전체 서버 상태와 폴링이 실패하지 않도록 세션별 오류 격리
- 서버 health v6의 `max_slot_manifest_count` 호환성 확인 추가
- 플러그인과 대시보드 표시 버전을 `v1.0.4`, 대시보드 배포 버전을 `v42.0.13`으로 동기화

## 42.0.12

- 플로팅 진행창의 z-index, 오른쪽/위쪽 여백(X/Y) 위치를 대시보드에서 조정 가능
- `항상 보이기 (위치 잡기용)` 옵션 추가: 활성 작업이 없어도 플로팅 창을 띄워 위치를 잡을 수 있도록 자리표시자 미리보기 표시
- 다른 플러그인 DOM과 겹칠 때 z-index를 낮춰 상대방 닫기 버튼이 가려지지 않도록 회피
- 위치 설정은 플러그인 저장소에 영속화하며 모듈 변경 없음
- 플러그인과 대시보드 표시 버전을 `v1.0.3`, 대시보드 배포 버전을 `v42.0.12`로 동기화

## 42.0.11

- Risu `SafeMutationObserver`가 텍스트 노드를 `SafeElement`로 감싸며 `getAttribute is not a function`을 발생시키는 `characterData` 관찰 옵션 제거
- 캐릭터 전환 감지는 안전한 `childList + subtree` DOM 신호와 일반 생성 직전 input 동기화로 유지
- 플러그인과 대시보드 표시 버전을 `v1.0.2`, 대시보드 배포 버전을 `v42.0.11`로 동기화

## 42.0.10

- 플러그인 저장소의 서버 주소를 기준으로 캐릭터 전환 시 현재 캐릭터 설정에 자동 동기화
- 기존 채팅에 남은 예전 주소보다 우선하는 `lb-xnai-server-endpoint-v2` 키를 추가하고, 호환용 기존 키도 함께 기록
- 일반 생성 직전의 변경 없는 input 신호와 기존 DOM 전환 감지를 함께 사용하며 네트워크 폴링은 추가하지 않음
- 모듈의 서버 주소 탐색 순서를 `v2 → 기존 키 → 모듈 수동 설정`으로 변경하고 manifest 조건부 1회 재시도는 유지

## 42.0.9

- Risu `request()`의 분당 제한을 고려해 슬롯 manifest가 HTTP 500 `fetch failed`를 반환할 때만 1.5초 후 한 번 재시도
- 정상 응답과 그 밖의 HTTP 오류에는 추가 manifest 요청을 보내지 않도록 제한

## 42.0.8

- 표시 이름을 `soya comfy manager plugin v1.0.1`로 변경
- 대시보드 접속 버튼을 `soya comfy 플러그인`으로 변경
- 대시보드 제목을 `soya comfy manager v1.0.1`로 변경

## 42.0.7

- 대시보드가 화면에 보이는 최신 HTTPS 주소를 직접 읽어 저장하도록 수정
- 현재 캐릭터 조회가 실패해도 플러그인 주소 저장과 새 주소 연결 확인을 계속하도록 수정
- 캐릭터 반영 실패를 서버 연결 실패와 분리해 안내하고, 다음 대시보드 진입 시 자동 재시도

## 42.0.6

- 표시 이름을 `soya comfymanager plugin v1.0.0`으로 변경
- 온라인 업데이트 식별자와 런타임 동작은 유지

## 42.0.5

- GitHub raw URL 기반 온라인 업데이트 메타데이터 추가
- 온라인 배포용 고정 파일명으로 저장소 구성
- 런타임 동작은 42.0.4와 동일
